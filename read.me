This is simple Terraform setup for AWS learning purposes. It creates EC2 instance and required resources like VPC, key pair, security group.

To run get repo and run `terraform init` and `terraform apply`.

